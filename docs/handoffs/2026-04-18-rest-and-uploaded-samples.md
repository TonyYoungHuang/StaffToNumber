# 2026-04-18 Rest Detection + Uploaded Sample Intake

## What changed

- improved rest detection so the targeted `barline-rest-context` fixture now reports stable `restCount` and `barlineCount`
- extended sample evaluation parsing to record `restCount` and `barlineCount` in `latest.json`
- tagged sample entries in `samples/manifest.json` with `sourceKind`
- continued intake of local uploaded PDFs from `services/api/storage/...` as sample fixtures, alongside composed multi-page / multi-staff fixtures

## Current sample sourcing

- `sourceKind: "uploaded_copy"`: copied from local uploaded PDFs already present in repo storage
- `sourceKind: "composed"`: generated by combining uploaded fixtures into multi-page or multi-staff layouts
- `sourceKind: "synthetic"`: targeted diagnostics such as blank pages and the barline/rest context fixture

## Validation

- `npm run evaluate:samples`
- current result: clean `11/11`, draft `6/6`, fail `3/3`

## Important current behavior

- `barline-rest-context` is still expected to stay `draft`
- it now exposes `rests: 4` and `bars: 2` in preview diagnostics, which makes it useful as a regression target for rest detection

## Best next steps

1. improve notehead recovery on barline-heavy pages so the barline/rest fixture can eventually become a full mixed-symbol sample
2. replace more composed fixtures with real user-uploaded PDFs when additional local assets become available
3. add dedicated rest-heavy fixtures for quarter/eighth rest shapes rather than only block-like placeholders
