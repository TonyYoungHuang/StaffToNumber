# Sample Reports

Generated evaluation reports are written here by `npm run evaluate:samples`.

- `latest.json` and `latest.md` are overwritten on each run
- `history/` keeps timestamped snapshots for regression tracking
